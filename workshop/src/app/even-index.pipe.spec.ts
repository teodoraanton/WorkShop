import { EvenIndexPipe } from './even-index.pipe';

describe('EvenIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new EvenIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
